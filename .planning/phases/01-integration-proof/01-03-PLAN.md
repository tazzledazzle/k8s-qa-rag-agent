---
phase: 01-integration-proof
plan: 01-03
type: execute
wave: 2
depends_on: []
requirements:
  - E2E-03
must_haves:
  truths:
    - "Default branch CI has no silent-success checks that claim strict typing without enforcing it."
    - "`pytest`, `flake8` (hard selectors), `black --check`, and all four `docker build` lines remain blocking on PR/push to main/develop."
    - "If mypy is not yet strict-clean, CI names the job **informational** and REQUIREMENTS.md E2E-03 wording is updated in the same PR to match the honest contract — OR mypy is fixed and `|| true` removed."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: Honest CI gates for Phase 1
    - path: .planning/REQUIREMENTS.md
      provides: E2E-03 text aligned with actual CI behavior (only if contract changes)
---

<objective>
Satisfy **E2E-03**: CI is a **credible regression gate** on clean checkout (`01-CONTEXT.md` D-09–D-12).

Purpose: Remove “green but meaningless” signals (notably **`mypy … || true`**), align coverage upload semantics, and ensure new Phase 1 scripts/docs do not break existing jobs.

Output: Updated workflow(s), optional `mypy.ini` / pyproject mypy section, and synchronized requirement text if the contract shifts.
</objective>

<context>
@.github/workflows/ci.yml
@.planning/REQUIREMENTS.md
@pyproject.toml
@services/
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Baseline mypy debt inventory</name>
  <files>services/</files>
  <read_first>pyproject.toml, setup.cfg, mypy.ini</read_first>
  <behavior>
    - Run `mypy services` locally and capture error count and top categories.
    - Decide **Path A**: fix errors incrementally until clean, or **Path B**: rename job to “mypy (informational)” and adjust REQUIREMENTS E2E-03 to “lint + tests + docker build” as the contract (explicitly excluding strict mypy until Phase 2+).
  </behavior>
  <action>Record decision in `01-03-SUMMARY.md` rationale section (1–2 paragraphs).</action>
  <verify>
    <automated>mypy services 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>Decision documented; team knows which path was taken.</acceptance_criteria>
  <done>Executor has explicit fix vs defer choice.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement chosen mypy policy in CI</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>Task 1 outcome</read_first>
  <behavior>
    - **Path A:** Remove `|| true`; mypy failures fail the job; optionally scope to `services/indexer` first if incremental.
    - **Path B:** `continue-on-error: true` **and** job `name:` includes “informational”; add workflow comment + REQUIREMENTS.md bullet edit.
  </behavior>
  <action>Patch `ci.yml` accordingly; keep runtime reasonable for GitHub-hosted runners.</action>
  <verify>
    <automated>act push</automated>
  </verify>
  <acceptance_criteria>CI YAML is valid; behavior matches chosen path.</acceptance_criteria>
  <done>No silent `|| true` masking without documentation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: codecov and coverage semantics</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml (codecov step)</read_first>
  <behavior>
    - `fail_ci_if_error: false` is acceptable for portfolio if documented; otherwise enable failure once token configured.
    - Ensure new tests from 01-01/01-02 are included in `pytest tests/` coverage scope.
  </behavior>
  <action>Adjust only if Phase 1 honesty requires it; avoid drive-by CI rewrites.</action>
  <verify>
    <manual>Inspect workflow diff</manual>
  </verify>
  <acceptance_criteria>Coverage upload behavior is intentional, not accidental.</acceptance_criteria>
  <done>E2E-03 clarity extends to coverage optional fail.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire new scripts into CI if appropriate</name>
  <files>.github/workflows/ci.yml, scripts/smoke_compose.sh</files>
  <read_first>01-01-PLAN.md outputs</read_first>
  <behavior>
    - Optionally add **`compose-smoke`** job using **Docker Compose v2** on ubuntu-latest: `docker compose up -d` + `scripts/smoke_compose.sh` **without** LLM key (health-only). Consider job timeout and ES startup cost.
    - If too slow/flaky for free tier, document skip in `01-03-SUMMARY.md` and rely on manual smoke + optional workflow dispatch.
  </behavior>
  <action>Only add job if flake budget is acceptable; otherwise defer to Phase 3 ops hardening.</action>
  <verify>
    <manual>Run job on branch</manual>
  </verify>
  <acceptance_criteria>Either compose-smoke runs in CI or deferral is documented with reason.</acceptance_criteria>
  <done>CI scope matches team tolerance for runner time.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: REQUIREMENTS sync (conditional)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md (E2E-03)</read_first>
  <behavior>
    - If E2E-03 text promises “mypy strict” but CI is informational, **edit E2E-03** to describe the actual gate set.
    - If Path A succeeded, reinforce E2E-03 with “mypy required” language.
  </behavior>
  <action>Minimal diff to requirements table / E2E-03 bullet only.</action>
  <verify>
    <manual>Traceability table still maps E2E-03 → Phase 1</manual>
  </verify>
  <acceptance_criteria>REQUIREMENTS.md does not contradict `.github/workflows/ci.yml`.</acceptance_criteria>
  <done>E2E-03 is legally honest.</done>
</task>

</tasks>

<verification>
- Push to branch and confirm **test** + **build** jobs green
- Local: `pytest tests/`, `flake8 …`, `black --check`, `mypy` per chosen policy
</verification>

<success_criteria>
- CI contract matches documented requirements.
- No undocumented `|| true` on quality gates.
</success_criteria>

<output>
After completion, create `01-03-SUMMARY.md` in this directory.
</output>

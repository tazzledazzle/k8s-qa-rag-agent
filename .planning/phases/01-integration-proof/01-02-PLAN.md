---
phase: 01-integration-proof
plan: 01-02
type: execute
wave: 2
depends_on:
  - 01-01
requirements:
  - E2E-02
must_haves:
  truths:
    - "A numbered runbook documents index → ask → verify citation (or explicit interim rule if citations list still empty)."
    - "`POST /ask` returns an answer grounded in retrieval for a fixture-scoped question when `OPENAI_API_KEY` is set."
    - "Citations in `AskResponse` include at least one entry whose `file_path` exists in the fixture when the agent used retrieval — OR runbook documents approved interim assertion on answer text (only if citation plumbing is impossible in Phase 1; default is to populate `citations`)."
  artifacts:
    - path: docs/PHASE1-RUNBOOK.md
      provides: Copy-paste steps for E2E-02 manual proof
---

<objective>
Prove **E2E-02**: **question → retrieval → answer** with **at least one correct file-path citation** aligned with `.planning/REQUIREMENTS.md` and `01-CONTEXT.md` D-05, D-06, D-08.

Purpose: Close the gap between “services run” and “product value is demonstrable.”

Primary deliverable: **`docs/PHASE1-RUNBOOK.md`** (numbered steps, exact JSON bodies, expected substrings). Optional: **`@pytest.mark.integration`** tests skipped by default in CI unless `RUN_COMPOSE_INTEGRATION=1`.
</objective>

<context>
@.planning/phases/01-integration-proof/01-CONTEXT.md
@services/qa_agent/main.py
@services/qa_agent/agent.py
@services/qa_agent/tools.py
@services/retriever/main.py
@docs/API.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Citation plumbing in QA agent</name>
  <files>services/qa_agent/agent.py, services/qa_agent/models.py</files>
  <read_first>services/qa_agent/agent.py (run_agent return path), services/retriever/main.py (SearchResponse schema)</read_first>
  <behavior>
    - `run_agent` currently returns **`citations=[]`**. Populate `AskResponse.citations` from **tool/retriever payloads** (e.g. parse `file_path` from latest `search_codebase` / `search_runbooks` tool JSON, dedupe, cap list length).
    - Fail soft: if parsing fails, return answer with empty citations but log warning — runbook must then use answer-text assertion as backup (document clearly).
  </behavior>
  <action>Implement minimal deterministic extraction (JSON parse of tool message content) without changing LangGraph topology unless necessary.</action>
  <verify>
    <automated>pytest tests/ -q -k qa</automated>
  </verify>
  <acceptance_criteria>Unit-level test with mocked retriever JSON proves at least one `Citation` is emitted when tool returns hits with `file_path`.</acceptance_criteria>
  <done>E2E-02 can assert structured citations, not only prose.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author PHASE1-RUNBOOK</name>
  <files>docs/PHASE1-RUNBOOK.md</files>
  <read_first>services/indexer/models.py, services/qa_agent/models.py</read_first>
  <behavior>
    - Steps: start compose → wait/smoke → `POST /index` with fixture `repo_url` from 01-01 → `POST /ask` with a question that references fixture symbol → verify **HTTP 200** and **citation `file_path`** matches fixture file.
    - Document **`OPENAI_API_KEY`** requirement and approximate wait for embedding model load on cold indexer (context D-06).
    - If CI cannot run LLM, state explicitly that **E2E-02 proof is manual or optional workflow job** (mirror CONTEXT).
  </behavior>
  <action>Create `docs/PHASE1-RUNBOOK.md`; link from README Phase 1 section.</action>
  <verify>
    <manual>Execute runbook once on developer machine</manual>
  </verify>
  <acceptance_criteria>Another engineer can follow runbook without reading Python source.</acceptance_criteria>
  <done>E2E-02 has a durable, reviewable procedure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Optional integration test hook</name>
  <files>tests/, pyproject.toml or pytest.ini</files>
  <read_first>tests/test_imports.py</read_first>
  <behavior>
    - Add `pytest.mark.integration` tests that call live `localhost` ports **only** when env `RUN_COMPOSE_INTEGRATION=1`.
    - Default CI remains fast; no network beyond local stack.
  </behavior>
  <action>Optional: one test that `POST /search` returns hits after index (skips if env unset). Document in runbook.</action>
  <verify>
    <automated>RUN_COMPOSE_INTEGRATION=1 pytest tests/ -m integration -q</automated>
  </verify>
  <acceptance_criteria>Default `pytest tests/` unchanged green; integration path documented.</acceptance_criteria>
  <done>Optional automation exists without slowing CI.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Optional CI job for LLM path</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <behavior>
    - Add job **`e2e-llm`** with `if: secrets.OPENAI_API_KEY != ''` (or repository variable) that starts compose, runs smoke, indexes fixture, curls `/ask`, greps citation — **only** when secret present.
    - Comment in YAML explains skip behavior (CONTEXT D-06).
  </behavior>
  <action>Keep job time bounded; reuse `may-portfolio-projects/k8s-qa-rag-agent` working-directory pattern.</action>
  <verify>
    <manual>Workflow validates in YAML + dry-run where possible</manual>
  </verify>
  <acceptance_criteria>Forks without secrets stay green; orgs with secrets get extra signal.</acceptance_criteria>
  <done>CI honestly reflects optional LLM E2E coverage.</done>
</task>

</tasks>

<verification>
- Manual: full `docs/PHASE1-RUNBOOK.md` on compose stack with API key
- `pytest tests/ -q` (default)
- Optional: `RUN_COMPOSE_INTEGRATION=1 pytest -m integration`
</verification>

<success_criteria>
- Runbook proves query path with citation or documented interim assertion.
- QA agent returns non-empty citations when retrieval returns structured paths.
</success_criteria>

<output>
After completion, create `01-02-SUMMARY.md` in this directory.
</output>

---
phase: 01-integration-proof
plan: 01-01
type: execute
wave: 1
depends_on: []
requirements:
  - E2E-01
must_haves:
  truths:
    - "A self-contained fixture repo exists in git and can be indexed without private GitHub credentials."
    - "`scripts/smoke_compose.sh` (or equivalent single entrypoint) exits 0 only when datastores and all four app `/health` endpoints return HTTP 200."
    - "Contributor docs list exact compose service names, ports, and prerequisite env vars for a cold start."
  artifacts:
    - path: tests/fixtures/sample-repo/
      provides: Minimal Python (and optional markdown) sources with a stable symbol name for later citation checks
    - path: scripts/smoke_compose.sh
      provides: Automated smoke for compose bring-up assumptions
---

<objective>
Satisfy **E2E-01** foundations: reproducible **fixture data**, **compose smoke automation**, and **README alignment** with real service names and ports (per `01-CONTEXT.md` D-01, D-02, D-03, D-04, D-07).

Purpose: Unblock **01-02** (query path proof) by guaranteeing indexable content and a reliable health gate.

Output: Fixture tree, indexer support for local/file-based repos if needed, smoke script, and concise README “Phase 1 / local dev” steps.
</objective>

<context>
@.planning/phases/01-integration-proof/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@docker-compose.yml
@services/indexer/git_client.py
@services/indexer/main.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add git-backed fixture repository</name>
  <files>tests/fixtures/sample-repo/</files>
  <read_first>services/indexer/git_client.py, services/indexer/chunker.py</read_first>
  <behavior>
    - Create **at least two** small source files (e.g. `sample_module.py` with a uniquely named function or constant, optional `README.md`).
    - Initialize a **real git repo** with `main` branch and at least one commit (scripted in Task 1 verify or documented `git init` steps in README).
    - Keep total size tiny; avoid large embeddings-only stress in Phase 1.
  </behavior>
  <action>Add `tests/fixtures/sample-repo/` with committed-style layout. If git metadata cannot be committed sanely, add `tests/fixtures/sample-repo/bootstrap.sh` that creates `.git` and an initial commit for local/CI use — document that smoke/E2E assumes this was run once.</action>
  <verify>
    <automated>test -d tests/fixtures/sample-repo/.git || test -f tests/fixtures/sample-repo/bootstrap.sh</automated>
  </verify>
  <acceptance_criteria>Fixture is a valid git tree after bootstrap; contains a greppable symbol name documented in plan 01-02 runbook.</acceptance_criteria>
  <done>Fixture exists and is referenced from README/smoke.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Indexer — index local/file-backed repos for fixtures</name>
  <files>services/indexer/git_client.py, services/indexer/models.py (optional)</files>
  <read_first>services/indexer/git_client.py (clone_or_update, _get_authenticated_url, _get_local_path)</read_first>
  <behavior>
    - Today only GitHub-shaped URLs work. Extend behavior so **`file://`** (absolute path to a local git repo) **or** a documented **`INDEXER_FIXTURE_REPO`** env consumed at index time can clone/open without GitHub.
    - `_get_local_path` must stay collision-safe for `file://` URLs (e.g. hashed subdirectory under `INDEXER_REPO_CACHE`).
    - Preserve existing GitHub flows unchanged for production URLs.
  </behavior>
  <action>Implement minimal branch: detect `repo_url.startswith("file:")` (and optionally bare POSIX path if you choose one convention only — prefer **single** convention and document it). Use `Repo.clone_from` / `Repo` as appropriate for `file://` remotes. Add unit tests for path derivation if practical without full git binary in unit test.</action>
  <verify>
    <automated>pytest tests/ -q -k git</automated>
  </verify>
  <acceptance_criteria>`POST /index` succeeds against fixture using documented `repo_url` without `GITHUB_TOKEN`.</acceptance_criteria>
  <done>Indexer can index the in-repo fixture from compose or host.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Compose volume mount for fixture path</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml, services/indexer/Dockerfile</read_first>
  <behavior>
    - Indexer container must see fixture at the **same absolute path** referenced in `IndexRequest.repo_url` `file://` URL (or mount point documented in README).
    - Do not mount entire host `$HOME`; scope read-only to fixture directory.
  </behavior>
  <action>Add `volumes:` entry on `indexer` service mounting `./tests/fixtures/sample-repo` to e.g. `/fixtures/sample-repo:ro`. Document exact `repo_url` string in README.</action>
  <verify>
    <manual>docker compose config</manual>
  </verify>
  <acceptance_criteria>`docker compose config` validates; indexer can read mounted fixture inside container.</acceptance_criteria>
  <done>Compose and indexer agree on fixture mount path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Smoke script for health endpoints</name>
  <files>scripts/smoke_compose.sh</files>
  <read_first>01-CONTEXT.md (D-07), docker-compose.yml port mappings</read_first>
  <behavior>
    - Poll **qdrant / redis / elasticsearch** health the same way compose does (or `docker compose ps` + curl) until ready or timeout (~120–180s for ES).
    - Curl `http://localhost:8000/health` … `8003/health` and require HTTP 200.
    - Bash `set -euo pipefail`; meaningful stderr on failure.
  </behavior>
  <action>Add `scripts/smoke_compose.sh` executable. Optionally wire `make smoke` in Makefile if one exists or is trivial to add.</action>
  <verify>
    <manual>docker compose up -d && ./scripts/smoke_compose.sh</manual>
  </verify>
  <acceptance_criteria>Script passes on healthy stack and fails fast when a service is down.</acceptance_criteria>
  <done>E2E-01 has an automated health gate for local compose.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: README — contributor bring-up (E2E-01)</name>
  <files>README.md (or docs/LOCAL-DEV.md if README is too large)</files>
  <read_first>README.md top sections</read_first>
  <behavior>
    - Document: `docker compose up -d` (or `--wait` if adopted), required env vars (`OPENAI_API_KEY` optional for health-only), ports **8000–8003**, service names (`qa-agent` vs import path `qa_agent`).
    - Link or embed one command block running smoke script.
  </behavior>
  <action>Add a short **“Local proof (Phase 1)”** section; avoid duplicating full design narrative.</action>
  <verify>
    <manual>Follow section on clean machine mental walkthrough</manual>
  </verify>
  <acceptance_criteria>New contributor can reach all four `/health` endpoints using only the doc + smoke script.</acceptance_criteria>
  <done>E2E-01 documentation matches reality.</done>
</task>

</tasks>

<verification>
- `docker compose config`
- `./scripts/smoke_compose.sh` (with stack up)
- `pytest tests/ -q` (after any new unit tests)
</verification>

<success_criteria>
- Fixture exists and is indexable via supported `repo_url` convention.
- Smoke script validates full compose health surface.
- README (or LOCAL-DEV) matches compose layout and ports.
</success_criteria>

<output>
After completion, create `01-01-SUMMARY.md` in this directory per GSD execute template (what changed, how verified, follow-ups for 01-02).
</output>

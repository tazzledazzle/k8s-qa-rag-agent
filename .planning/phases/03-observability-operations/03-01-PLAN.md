---
phase: 03-observability-operations
plan: 03-01
type: execute
wave: 1
depends_on: []
requirements:
  - OBS-01
must_haves:
  truths:
    - "Every `POST /ask` and `POST /search` request gets a `correlation_id` (UUID) at the HTTP boundary and that value appears in structured (JSON) logs for both services."
    - "QA agent passes `X-Correlation-ID` to the retriever on `httpx` calls; retriever logs the same id when the header is present."
    - "Automated tests prove header propagation without requiring a live OpenAI call (use `/search` directly and/or FastAPI `TestClient` + mocked agent path)."
  artifacts:
    - path: services/qa_agent/main.py
      provides: middleware + logging setup
    - path: services/retriever/main.py
      provides: middleware + logging setup
    - path: services/qa_agent/tools.py
      provides: outbound correlation header
    - path: tests/test_correlation_id.py
      provides: pytest coverage for OBS-01
---

<objective>
Satisfy **OBS-01** per `03-CONTEXT.md` **D-01–D-04**: **structured JSON logs** with **`correlation_id`** on **qa-agent** and **retriever**, and **propagation** across the internal HTTP hop.

Output: working middleware + `contextvars` (or equivalent), `X-Correlation-ID` on retriever requests from tools, and **pytest** that locks the contract. **OTEL** spans and collector wiring are explicitly **out of this plan** (see **03-02**).
</objective>

<context>
@.planning/phases/03-observability-operations/03-CONTEXT.md
@.planning/REQUIREMENTS.md
@services/qa_agent/main.py
@services/qa_agent/tools.py
@services/qa_agent/agent.py
@services/retriever/main.py
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: JSON logging bootstrap (both services)</name>
  <files>services/qa_agent/main.py, services/retriever/main.py</files>
  <read_first>pyproject.toml (python-json-logger already listed)</read_first>
  <behavior>
    - Configure root/service loggers to emit **one JSON object per line** to stdout (Kubernetes-friendly). Use **`pythonjsonlogger.jsonlogger.JsonFormatter`** with a stable field set including **`level`**, **`message`**, **`logger`**, **`time`** (ISO8601).
    - Avoid printing secrets; do not log `OPENAI_API_KEY` or request bodies wholesale.
    - Keep **`/health`** logs minimal (optional debug) — focus structured fields on **`POST /ask`** and **`POST /search`** lifecycle.
  </behavior>
  <action>Add a small internal helper module only if duplication exceeds ~30 lines; otherwise configure inline in each `main.py`.</action>
  <verify>
    <automated>python -m pytest tests/ -q --tb=short</automated>
  </verify>
  <acceptance_criteria>Running either service locally prints JSON log lines when hitting `/search` or `/ask`.</acceptance_criteria>
  <done>Baseline structured logging exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: correlation_id context + FastAPI middleware</name>
  <files>services/qa_agent/main.py, services/retriever/main.py</files>
  <read_first>FastAPI middleware docs (ASGI), contextvars</read_first>
  <behavior>
    - **`contextvars.ContextVar[str]`** (e.g. `correlation_id_var`) set in middleware for each request.
    - On incoming request: if **`X-Correlation-ID`** header is present and non-empty, reuse it (trim); else generate **UUID4** string.
    - Log **`request_started`** / **`request_finished`** (or equivalent messages) including **`correlation_id`**, method, path, status code, duration_ms.
    - Apply middleware to **all** routes (including `/health`) is acceptable; ensure **`correlation_id`** is always defined for the request scope.
  </behavior>
  <action>Use `Starlette` middleware pattern compatible with FastAPI.</action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>Manual curl shows JSON logs containing matching `correlation_id` for a single `/search` call.</acceptance_criteria>
  <done>D-01 satisfied for HTTP boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Propagate X-Correlation-ID from QA agent to retriever</name>
  <files>services/qa_agent/tools.py</files>
  <read_first>services/qa_agent/main.py (context var), services/qa_agent/tools.py</read_first>
  <behavior>
    - In **`_search`**, read current **`correlation_id_var`** (fallback: generate UUID if unset — should not happen for normal `/ask` path).
    - Add HTTP header **`X-Correlation-ID`** with that value on **`httpx` POST** to retriever.
    - Do not break tests that import `_search` directly — tests may set the contextvar explicitly.
  </behavior>
  <action>Minimal diff to `httpx.Client.post(..., headers={...})`.</action>
  <verify>
    <automated>pytest tests/test_correlation_id.py -q</automated>
  </verify>
  <acceptance_criteria>Retriever logs show the **same** `correlation_id` as QA emitted when chaining requests.</acceptance_criteria>
  <done>D-02 satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Retriever uses inbound X-Correlation-ID</name>
  <files>services/retriever/main.py</files>
  <read_first>middleware order (correlation before route)</read_first>
  <behavior>
    - Middleware must prefer incoming **`X-Correlation-ID`** when valid (non-empty string after trim); otherwise generate UUID as in Task 2.
    - Ensure **`logger.warning`** calls in `_hybrid_search` (dense/BM25 failure branches) include **`correlation_id`** in **`extra=`** so the JSON formatter records it.
  </behavior>
  <action>Keep log volume stable — only add `correlation_id` to warning/error paths if info-level spam is a concern.</action>
  <verify>
    <automated>pytest tests/test_correlation_id.py -q</automated>
  </verify>
  <acceptance_criteria>When `X-Correlation-ID` is sent, retriever JSON logs use that value, not a fresh UUID.</acceptance_criteria>
  <done>D-02 + D-03 for retriever degradation visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Pytest contract for OBS-01</name>
  <files>tests/test_correlation_id.py</files>
  <read_first>tests/conftest.py if present; services/retriever/models.py for SearchRequest</read_first>
  <behavior>
    - **Case A:** `TestClient` against retriever: POST `/search` without header → response 200; use **`caplog`** with JSON formatter — assert **`correlation_id`** present in a log record.
    - **Case B:** POST `/search` **with** `X-Correlation-ID: test-fixed-id` → logs contain **`test-fixed-id`**.
    - **Case C (integration-light):** `TestClient` qa-agent `POST /ask` with **mocked** `run_agent` returning a minimal `AskResponse` — patch `run_agent` to avoid OpenAI; assert outbound retriever call carried **`X-Correlation-ID`** (e.g. **`httpx.MockTransport`** or monkeypatch capturing headers).
  </behavior>
  <action>New test file; no network by default.</action>
  <verify>
    <automated>pytest tests/test_correlation_id.py -v</automated>
  </verify>
  <acceptance_criteria>OBS-01 is regression-locked in CI.</acceptance_criteria>
  <done>Automated proof for verifier.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Docs touch-up (OBS-01 operator hint)</name>
  <files>docs/OPERATIONS.md or README.md</files>
  <read_first>docs/SECURITY-SECRETS.md (style)</read_first>
  <behavior>
    - One short subsection: **Correlation IDs** — log field name, header name, which services participate.
  </behavior>
  <action>≤15 lines unless tables are clearer.</action>
  <verify>
    <manual>Human read for accuracy</manual>
  </verify>
  <acceptance_criteria>Operators know what to grep in Loki or `kubectl logs`.</acceptance_criteria>
  <done>D-04 documentation minimum.</done>
</task>

</tasks>

<execution_notes>
- **Order:** Task 1 → 2 → 3 → 4 → 5 → 6. Tasks 3–5 are the critical path for TDD.
- **Compose:** no compose file change required unless uvicorn log config needs an env toggle — prefer zero new env vars; if `LOG_JSON=true` is introduced, document in **Task 6**.
- **Next plan:** **03-02** after this wave is verified green (`depends_on` for OTEL/docs).
</execution_notes>

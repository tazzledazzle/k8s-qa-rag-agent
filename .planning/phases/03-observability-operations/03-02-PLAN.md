---
phase: 03-observability-operations
plan: 03-02
type: execute
wave: 2
depends_on:
  - 03-01
requirements:
  - OBS-02
  - OBS-03
must_haves:
  truths:
    - "OpenTelemetry Collector config in `k8s/base/configmap-otel.yaml` parses (`otelcol validate`) and is mounted by the qa-agent sidecar; pipelines do not self-export OTLP to the same receiver in a broken loop unless explicitly justified and documented."
    - "QA agent (and retriever if instrumented) document `OTEL_*` env for Kubernetes sidecar and local dev; prod overlay does not contradict ports or pinned collector image."
    - "`docs/OPERATIONS.md` includes index lag, Elasticsearch yellow, and Qdrant disk pressure remediation plus a validation checklist with at least one executed drill recorded for `03-VERIFICATION.md`."
  artifacts:
    - path: k8s/base/configmap-otel.yaml
      provides: valid collector config
    - path: k8s/base/qa-agent-deployment.yaml
      provides: sidecar args + volumeMount + app env
    - path: .github/workflows/ci.yml
      provides: non-flaky config validation step (optional second job)
    - path: docs/OPERATIONS.md
      provides: failure modes + checklist
---

<objective>
Satisfy **OBS-02** and **OBS-03** per `03-CONTEXT.md` **D-05–D-12**: fix **in-cluster OTEL** wiring and documentation, add **automated config validation**, extend **`docs/OPERATIONS.md`** with roadmap failure modes, and capture **one manual validation pass** evidence for the verifier.

**Depends on:** **03-01** complete (correlation IDs stable — traces can reuse the same `correlation_id` as span attribute if desired).

Output: working collector sidecar + validated YAML; minimal Python **OTLP** export proving traces reach the collector in a documented dry-run; expanded operations runbook + checklist.
</objective>

<context>
@.planning/phases/03-observability-operations/03-CONTEXT.md
@.planning/phases/03-observability-operations/03-01-PLAN.md
@k8s/base/configmap-otel.yaml
@k8s/base/qa-agent-deployment.yaml
@k8s/base/kustomization.yaml
@k8s/overlays/prod/kustomization.yaml
@services/qa_agent/main.py
@services/retriever/main.py
@docs/OPERATIONS.md
@docs/DESIGN.md
@pyproject.toml
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace collector config with a known-good pipeline</name>
  <files>k8s/base/configmap-otel.yaml</files>
  <read_first>Collector OTLP receiver/exporter semantics; `03-CONTEXT` D-05</read_first>
  <behavior>
    - **Remove** the **`exporters.otlp` → `localhost:4317`** loop unless replaced with a deliberate **second** collector or upstream URL documented as out-of-repo.
    - Minimum acceptable portfolio config: **OTLP in** → **batch** → **`debug`** and/or **`logging`** exporter for traces/metrics proof; optional **Prometheus** self-scrape on `:8888` for the collector only.
    - Keep **receivers.otlp** on **`0.0.0.0:4317`** (gRPC) aligned with sidecar `containerPort`.
  </behavior>
  <action>Patch ConfigMap data only; keep name `otel-collector-config` unless kustomize references must change.</action>
  <verify>
    <automated>Task 2 validator (or `docker run ... validate` locally)</automated>
  </verify>
  <acceptance_criteria>D-05: config is internally consistent.</acceptance_criteria>
  <done>Collector YAML is trustworthy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CI — `otelcol validate` on extracted config</name>
  <files>.github/workflows/ci.yml, scripts/validate-otel-config.sh (optional)</files>
  <read_first>Existing `test` job `working-directory` layout</read_first>
  <behavior>
    - Add a **fast** job or step that runs the upstream collector image with **`validate --config=/tmp/config.yaml`**, where `/tmp/config.yaml` is the same semantic content as the ConfigMap (either **checked-in mirror** under `scripts/otel/` or **heredoc** generated in CI from `k8s/base/configmap-otel.yaml` via small script).
    - Fail the job if validate exits non-zero.
  </behavior>
  <action>Prefer a small script committed to the repo so local dev matches CI.</action>
  <verify>
    <automated>Run script locally + CI green on branch</automated>
  </verify>
  <acceptance_criteria>D-08 satisfied.</acceptance_criteria>
  <done>OBS-02 automated gate exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mount ConfigMap + run sidecar with `--config`</name>
  <files>k8s/base/qa-agent-deployment.yaml</files>
  <read_first>Volume + volumeMount patterns in other Deployments</read_first>
  <behavior>
    - **`otel-collector-sidecar`**: `args: ["--config=/etc/otelcol/config.yaml"]` (or path matching mount).
    - **`volumeMount`** read-only ConfigMap key **`otel-collector-config.yaml`** → mounted path.
    - **`qa-agent` container**: set **`OTEL_EXPORTER_OTLP_ENDPOINT`** (and **`OTEL_SERVICE_NAME`**, **`OTEL_RESOURCE_ATTRIBUTES`**) to target **`http://127.0.0.1:4317`** (gRPC) per D-06.
  </behavior>
  <action>Ensure pod has **two** containers sharing network namespace (default) so localhost reaches sidecar.</action>
  <verify>
    <manual>`kubectl kustomize k8s/base` renders expected volumes/env</manual>
  </verify>
  <acceptance_criteria>Sidecar no longer runs implicit default config.</acceptance_criteria>
  <done>D-05 wiring complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Python OTLP + FastAPI instrumentation (qa-agent; retriever optional)</name>
  <files>services/qa_agent/main.py, services/retriever/main.py, pyproject.toml</files>
  <read_first>Existing `opentelemetry-*` deps in pyproject.toml</read_first>
  <behavior>
    - Initialize **TracerProvider** + **OTLP span exporter** gated on env (e.g. export only when **`OTEL_EXPORTER_OTLP_ENDPOINT`** is set) to avoid breaking unit tests that expect no network.
    - Use **`opentelemetry.instrumentation.fastapi.FastAPIInstrumentor.instrument_app(app)`** (or manual spans) so at least **`/ask`** and **`/search`** create spans.
    - Optionally attach **`correlation_id`** from **03-01** as a span attribute for join queries.
    - **Retriever:** mirror instrumentation if low-effort; otherwise document “qa-agent only” in **Task 6** with rationale (still satisfy OBS-02 if collector receives qa traffic only — plan-phase should prefer **both** HTTP services for symmetry).
  </behavior>
  <action>Keep imports lazy or startup-only to control cold-start cost.</action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>Dry-run: run qa-agent with sidecar locally or in k8s; collector **debug** logs show an **incoming trace** for `POST /ask` (document command in Task 6).</acceptance_criteria>
  <done>D-07 satisfied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Prod overlay — pin collector image tag</name>
  <files>k8s/overlays/prod/kustomization.yaml, optional patch file</files>
  <read_first>`k8s/base/qa-agent-deployment.yaml` image string</read_first>
  <behavior>
    - Replace **`otel/opentelemetry-collector-k8s:latest`** in **prod** overlay with a **specific semver tag** documented in commit message / plan summary (D-09, REL-01 spirit).
    - Base may stay on `latest` **only** if README calls it dev-only — prefer pinning in **base** too when low risk.
  </behavior>
  <action>JSON6902 or `images:` transformer per existing overlay conventions.</action>
  <verify>
    <automated>kubectl kustomize k8s/overlays/prod</automated>
  </verify>
  <acceptance_criteria>Prod render shows pinned collector tag.</acceptance_criteria>
  <done>Prod posture improved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Document OTEL ports and compose/local dev</name>
  <files>docs/OPERATIONS.md, docs/LOCAL-DEV.md, or new `docs/OTEL.md` + README link</files>
  <read_first>`03-CONTEXT` D-06</read_first>
  <behavior>
    - Table: **Variable**, **Meaning**, **Compose**, **Kubernetes (qa-agent pod)**.
    - Document **`4317` gRPC** vs **`4318` HTTP** if HTTP receiver enabled.
    - Cross-link **`configmap-otel.yaml`** and **sidecar** behavior.
  </behavior>
  <action>Keep DRY with `SECURITY-SECRETS` tone — facts, not marketing.</action>
  <verify>
    <manual>Technical review</manual>
  </verify>
  <acceptance_criteria>D-06 satisfied; OBS-02 doc half complete.</acceptance_criteria>
  <done>Operators can wire collectors without reading Python.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: OPERATIONS.md — failure modes + remediation</name>
  <files>docs/OPERATIONS.md</files>
  <read_first>README Elasticsearch `vm.max_map_count` note; existing retriever degradation section</read_first>
  <behavior>
    - Add subsections: **Index lag** (symptoms: stale answers; checks: indexer logs, CronJob, webhook delivery, `POST /index` manual); **Elasticsearch yellow** (single-node, shard allocation, disk watermark, `_cluster/health`); **Qdrant disk / collection growth** (volume sizing, delete collection vs re-index cost).
    - Cross-link **`docs/SECURITY-SECRETS.md`** for webhook secret misconfig / 403 rate (D-12).
  </behavior>
  <action>Concrete `curl` / `kubectl` examples where safe (no secrets).</action>
  <verify>
    <manual>Engineer other than author reads for clarity</manual>
  </verify>
  <acceptance_criteria>D-10 satisfied on paper.</acceptance_criteria>
  <done>OBS-03 content exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Runbook checklist + validation record stub</name>
  <files>docs/OPERATIONS.md, `.planning/phases/03-observability-operations/03-VERIFICATION.md` (create stub if missing)</files>
  <read_first>`03-CONTEXT` D-11</read_first>
  <behavior>
    - Append a **markdown table**: Failure mode | Detection | Mitigation | Validated (date / env).
    - Executing engineer fills **Validated** column during **`/gsd-verify-phase 3`**; for **execute**, add **`Not run`** placeholders.
  </behavior>
  <action>Do not fake validation dates.</action>
  <verify>
    <manual>N/A</manual>
  </verify>
  <acceptance_criteria>D-11 structure ready for verifier.</acceptance_criteria>
  <done>Checklist exists.</done>
</task>

</tasks>

<execution_notes>
- **Wave order:** Task 1–3 before Task 4 (collector must accept traffic). Task 2 can land early if using mirrored config file.
- **Compose:** optional follow-up — add lightweight `otel-collector` service to `docker-compose.yml` only if execute wants parity with k8s; not required if docs document k8s-first proof.
- **OBS-03 manual drills** are intentionally completed in **verify** (`/gsd-verify-phase 3`) to avoid lying in execute summaries.
</execution_notes>

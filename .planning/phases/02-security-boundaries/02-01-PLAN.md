---
phase: 02-security-boundaries
plan: 02-01
type: execute
wave: 1
depends_on: []
requirements:
  - SEC-01
  - SEC-03
must_haves:
  truths:
    - "Automated tests prove invalid or missing `X-Hub-Signature-256` never reaches indexer trigger logic."
    - "`GithubSignatureVerifier` behavior for edge inputs (empty secret, bad hex, wrong MAC) is covered by JVM unit tests."
    - "Git-watcher rejects `clone_url` values that violate the same allowlist policy documented for the indexer (defense in depth), before any HTTP call to `/index`."
  artifacts:
    - path: services/git-watcher/src/test/
      provides: JUnit tests for verifier + webhook route
    - path: services/git-watcher/build.gradle.kts
      provides: test dependencies (ktor-server-tests, kotlin-test)
---

<objective>
Satisfy **SEC-01** (webhook HMAC with negative proof) and the **git-watcher** half of **SEC-03** (clone URL policy before `IndexerClient.triggerIndex`), per `02-CONTEXT.md` **D-01–D-05**, **D-13**.

Output: testable git-watcher module, optional small refactors for test seams, and Kotlin tests runnable via **`./gradlew test`** in `services/git-watcher/`.
</objective>

<context>
@.planning/phases/02-security-boundaries/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@services/git-watcher/src/main/kotlin/com/k8sqarag/gitwatcher/GitWatcherApp.kt
@services/git-watcher/src/main/kotlin/com/k8sqarag/gitwatcher/GithubSignatureVerifier.kt
@services/git-watcher/src/main/kotlin/com/k8sqarag/gitwatcher/IndexerClient.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gradle test stack for git-watcher</name>
  <files>services/git-watcher/build.gradle.kts</files>
  <read_first>services/git-watcher/build.gradle.kts</read_first>
  <behavior>
    - Add **`testImplementation`** deps: Ktor test host (`io.ktor:ktor-server-test-host`), Kotlin test (JUnit 5 platform), and JSON fixtures as needed.
    - Wire **`tasks.test { useJUnitPlatform() }`** if using JUnit 5.
  </behavior>
  <action>Patch `build.gradle.kts`; keep JVM 21 and shadowJar behavior unchanged.</action>
  <verify>
    <automated>cd services/git-watcher && ./gradlew test</automated>
  </verify>
  <acceptance_criteria>`./gradlew test` runs (may be 0 tests until Task 2–3 land).</acceptance_criteria>
  <done>Test task is green on an empty or smoke test suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for GithubSignatureVerifier</name>
  <files>services/git-watcher/src/test/kotlin/com/k8sqarag/gitwatcher/GithubSignatureVerifierTest.kt</files>
  <read_first>GithubSignatureVerifier.kt</read_first>
  <behavior>
    - Cases: **empty secret** + valid-looking header ⇒ false; **null** header ⇒ false; **wrong prefix** (not `sha256=`) ⇒ false; **odd-length hex** ⇒ false; **wrong MAC** for payload ⇒ false; **correct MAC** ⇒ true.
    - Use fixed UTF-8 payload bytes and precomputed HMAC from known secret for the positive case.
  </behavior>
  <action>Create JUnit tests under conventional `src/test/kotlin/...` package mirroring main.</action>
  <verify>
    <automated>./gradlew test --tests GithubSignatureVerifierTest</automated>
  </verify>
  <acceptance_criteria>All verifier tests pass; no network I/O.</acceptance_criteria>
  <done>SEC-01 crypto edge cases are regression-locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor for testable webhook + indexer trigger</name>
  <files>services/git-watcher/src/main/kotlin/com/k8sqarag/gitwatcher/*.kt</files>
  <read_first>GitWatcherApp.kt, IndexerClient.kt</read_first>
  <behavior>
    - Introduce a narrow abstraction (e.g. **`fun interface IndexTrigger`** or class) used by the route so tests can substitute a **no-op / recording** implementation and assert it is **never** called on 401 paths.
    - Keep production wiring defaulting to current **`IndexerClient.triggerIndex`** behavior.
  </behavior>
  <action>Minimal public API surface change; avoid broad DI framework unless already present.</action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <acceptance_criteria>Production `main` path unchanged for operators; tests compile.</acceptance_criteria>
  <done>Webhook tests can assert “no index trigger” without real HTTP to indexer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Ktor application tests for POST /webhook</name>
  <files>services/git-watcher/src/test/kotlin/com/k8sqarag/gitwatcher/WebhookRouteTest.kt</files>
  <read_first>GitWatcherApp.kt, models.kt</read_first>
  <behavior>
    - Use **`testApplication`** (or equivalent) to POST a minimal valid **`push`** JSON body with **`X-GitHub-Event: push`** and **`refs/heads/main`**.
    - **401** when signature missing / wrong; **200** or **202** per existing semantics when signature valid (and trigger mock invoked once for push).
    - Non-**`push`** events remain **ignored** (existing **202 Accepted** behavior — assert unchanged).
  </behavior>
  <action>Build minimal JSON string fixtures inline or in test resources.</action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <acceptance_criteria>SEC-01 “invalid signature never triggers indexing work” is enforced by tests.</acceptance_criteria>
  <done>Route-level regression coverage for webhook edge.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Repo clone URL allowlist (Kotlin) before indexer call</name>
  <files>services/git-watcher/src/main/kotlin/com/k8sqarag/gitwatcher/RepoAllowlist.kt (new), GitWatcherApp.kt</files>
  <read_first>02-CONTEXT.md (D-11, D-13)</read_first>
  <behavior>
    - Implement **`RepoAllowlist.matches(cloneUrl: String): Boolean`** reading the **same env var names** as the indexer will use in **02-02** (e.g. **`INDEXER_ALLOWED_GITHUB_ORGS`**, **`INDEXER_ALLOWED_REPO_URLS`**) — document exact matching rules in **`docs/SECURITY-SECRETS.md`** in plan **02-02** if not yet written; interim comment in Kotlin is OK.
    - **Permissive mode:** when both lists are **empty/unset** ⇒ allow all **https://github.com/** URLs (preserve portfolio dev UX); **restrictive** when either list non-empty.
    - **`file://`**: deny in git-watcher unless explicitly allowed (mirror indexer flag **`INDEXER_ALLOW_FILE_REMOTES`** or deny always here — **pick one** and align with **02-02**; default recommend **deny `file://` in watcher** since GitHub webhooks should only send HTTPS URLs).
    - On mismatch: respond **403** or **404** (choose one, document) and **do not** call index trigger.
  </behavior>
  <action>Add unit tests for allowlist parsing edge cases (commas, whitespace, trailing slashes).</action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <acceptance_criteria>Git-watcher blocks disallowed `clone_url` before indexer HTTP; tests document policy.</acceptance_criteria>
  <done>Watcher-side SEC-03 enforcement is in place.</done>
</task>

</tasks>

<verification>
- `cd services/git-watcher && ./gradlew test`
- `docker build -f services/git-watcher/Dockerfile …` (optional local smoke)
</verification>

<success_criteria>
- SEC-01 backed by failing tests if HMAC check is removed or bypassed.
- SEC-03: git-watcher enforces documented URL policy before `triggerIndex`.
</success_criteria>

<output>
After completion, create `02-01-SUMMARY.md` in this directory.
</output>

---
phase: 02-security-boundaries
plan: 02-02
type: execute
wave: 2
depends_on:
  - 02-01
requirements:
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "`docs/SECURITY-SECRETS.md` lists every sensitive env var, owning service, and how compose vs Kubernetes supply it — no mystery keys."
    - "No committed YAML or markdown contains live API keys; `secretKeyRef` usage is audited across `k8s/`."
    - "`POST /index` and `batch_index` entrypoints reject out-of-policy `repo_url` before clone (including `file://` rules aligned with Phase 1 fixture docs)."
  artifacts:
    - path: docs/SECURITY-SECRETS.md
      provides: SEC-02 inventory + operator matrix
    - path: services/indexer/repo_allowlist.py (or equivalent)
      provides: Shared allowlist checks for HTTP + batch
---

<objective>
Satisfy **SEC-02** (secrets inventory + manifest/compose alignment) and the **indexer / batch** half of **SEC-03**, per `02-CONTEXT.md` **D-06–D-14**.

Output: `docs/SECURITY-SECRETS.md`, indexer allowlist enforcement + pytest coverage, `.env.example` / compose / `k8s/` consistency pass, and optional **prod overlay** tweak for required webhook secret (**D-07**).
</objective>

<context>
@.planning/phases/02-security-boundaries/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@services/indexer/main.py
@services/indexer/batch_index.py
@services/indexer/git_client.py
@k8s/
@docker-compose.yml
@.env.example
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author SECURITY-SECRETS.md</name>
  <files>docs/SECURITY-SECRETS.md, README.md (one-line link)</files>
  <read_first>k8s/base/*deployment*.yaml, docker-compose.yml, .env.example</read_first>
  <behavior>
    - Table columns: **Variable**, **Service(s)**, **Purpose**, **Compose source**, **Kubernetes Secret/key** (or “N/A in base”).
    - Include **`OPENAI_API_KEY`**, **`GITHUB_WEBHOOK_SECRET`**, **`GITHUB_TOKEN`**, **`QDRANT_API_KEY`**, **`REDIS_PASSWORD`** (or document “unused if empty”).
    - Call out **`optional: true`** on webhook `secretKeyRef` and what that means for misconfiguration risk (**D-07**).
    - Document **allowlist env vars** (`INDEXER_ALLOWED_*`, `INDEXER_ALLOW_FILE_REMOTES`, path prefixes) and parity with git-watcher (**02-01**).
  </behavior>
  <action>Create doc; add README link under Configuration or Security one-liner.</action>
  <verify>
    <manual>Review for accidental real-looking tokens</manual>
  </verify>
  <acceptance_criteria>Another engineer can wire secrets without reading Python/Kotlin source.</acceptance_criteria>
  <done>SEC-02 inventory anchor exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repo hygiene grep + manifest audit</name>
  <files>k8s/**/*.yaml, docs/**/*.md</files>
  <read_first>docs/SECURITY-SECRETS.md</read_first>
  <behavior>
    - Search for suspicious literals (`sk-`, long base64 blobs in examples); redact or replace with placeholders like **`REPLACE_ME`** only where a literal is pedagogically needed.
    - Confirm sensitive env vars in Deployments use **`valueFrom.secretKeyRef`** (not inline strings). Flag exceptions in SECURITY doc.
    - Remove **unused** secret env injection from indexer if **`GITHUB_WEBHOOK_SECRET`** is not read by indexer code (inventory should reflect reality).
  </behavior>
  <action>Minimal YAML edits; prefer documentation over behavior change unless indexer truly unused.</action>
  <verify>
    <automated>rg -n "sk-[A-Za-z0-9]{10,}" . || true</automated>
  </verify>
  <acceptance_criteria>No new plaintext secrets committed; indexer manifest matches code.</acceptance_criteria>
  <done>SEC-02 grep + k8s alignment pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Indexer allowlist module + HTTP 403 on violation</name>
  <files>services/indexer/repo_allowlist.py, services/indexer/main.py</files>
  <read_first>git_client.py (_get_authenticated_url, _is_file_remote)</read_first>
  <behavior>
    - **`INDEXER_ALLOWED_GITHUB_ORGS`**: comma-separated GitHub org/login (case-insensitive compare to URL owner).
    - **`INDEXER_ALLOWED_REPO_URLS`**: comma-separated full HTTPS URLs (normalize trailing `.git`, compare after canonicalization).
    - **Permissive:** both empty ⇒ allow any **`https://github.com/`** remote; **restrictive** if either set.
    - **`file://`**: require **`INDEXER_ALLOW_FILE_REMOTES=true`** and path under one of **`INDEXER_ALLOWED_FILE_PATH_PREFIXES`** (default single prefix **`/fixtures`** for compose doc — finalize in code comments + SECURITY doc).
    - Reject with **`HTTPException(403)`** (or **422** — pick one and document) **before** `git.clone_or_update`.
  </behavior>
  <action>Pure functions + small unit surface; keep `GitClient` free of policy except optional call from `main`.</action>
  <verify>
    <automated>pytest tests/ -q -k allowlist</automated>
  </verify>
  <acceptance_criteria>Table-driven tests cover allow/deny for GitHub + `file://` flag behavior.</acceptance_criteria>
  <done>Indexer SEC-03 enforcement for `/index`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: batch_index uses same allowlist</name>
  <files>services/indexer/batch_index.py</files>
  <read_first>services/indexer/batch_index.py, repo_allowlist.py</read_first>
  <behavior>
    - Before building **`IndexRequest`**, validate **`INDEX_REPO_URL`** with the **same** helper as HTTP path (**D-14**).
    - Fail fast with non-zero exit and clear log line on violation.
  </behavior>
  <action>Thin wrapper call; no duplicate policy strings.</action>
  <verify>
    <automated>pytest tests/ -q -k batch</automated>
  </verify>
  <acceptance_criteria>Test or subprocess-style check documents deny path (lightweight).</acceptance_criteria>
  <done>CronJob / batch path cannot bypass allowlist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Compose + .env.example alignment</name>
  <files>docker-compose.yml, .env.example, docs/LOCAL-DEV.md, docs/PHASE1-RUNBOOK.md</files>
  <read_first>docs/SECURITY-SECRETS.md</read_first>
  <behavior>
    - Add allowlist + file-remote env vars to **`.env.example`** with safe defaults matching **permissive GitHub + file allowed for fixture** story for local dev.
    - **`docker-compose.yml`**: pass through needed vars to **indexer** and **git-watcher** so Phase 1 **`file:///fixtures/...`** still works when flags set.
    - Update **LOCAL-DEV** / **PHASE1-RUNBOOK** only if commands or required env vars change.
  </behavior>
  <action>Minimal diffs; document breaking change for unrestricted prod-like setups.</action>
  <verify>
    <automated>docker compose config -q</automated>
  </verify>
  <acceptance_criteria>Compose config valid; fixture path still documented.</acceptance_criteria>
  <done>Operator story matches code defaults.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Optional prod overlay — required webhook secret</name>
  <files>k8s/overlays/prod/*.yaml</files>
  <read_first>k8s/overlays/prod/kustomization.yaml, git-watcher-deployment in base</read_first>
  <behavior>
    - If feasible with Kustomize strategic merge, set **`optional: false`** (or remove optional) for **`GITHUB_WEBHOOK_SECRET`** on **git-watcher** in **prod** only; document in SECURITY doc.
    - If merge is awkward, document “manual patch required” in SECURITY instead of fragile YAML.
  </behavior>
  <action>Prefer small patch file over copying whole Deployment.</action>
  <verify>
    <automated>kubectl kustomize k8s/overlays/prod >/dev/null</automated>
  </verify>
  <acceptance_criteria>Prod kustomize builds; behavior documented.</acceptance_criteria>
  <done>D-07 prod posture addressed or explicitly deferred with reason.</done>
</task>

</tasks>

<verification>
- `pytest tests/`
- `mypy services`
- `kubectl kustomize k8s/base` and `k8s/overlays/prod`
- `docker compose config -q`
</verification>

<success_criteria>
- SEC-02: inventory + no stray secrets in tree.
- SEC-03: indexer + batch enforce policy; compose/k8s wired for documented defaults.
</success_criteria>

<output>
After completion, create `02-02-SUMMARY.md` in this directory.
</output>

---
phase: 04-documentation-release
plan: 04-02
type: execute
wave: 2
depends_on:
  - 04-01
requirements:
  - REL-01
must_haves:
  truths:
    - "A single doc (`docs/RELEASE.md` or README section) lists all four service Dockerfiles, names the CI `build` job step, and explains how prod should pin images (tag or digest) — verifier can answer: what tag and where."
    - "`kubectl kustomize k8s/overlays/prod` renders explicit image references for qa-agent, retriever, indexer, and git-watcher that are not naked `latest` **or** the doc states an approved external CD pattern (Flux) as the pinning mechanism."
    - "Qdrant image drift is explicitly addressed: prod overlay pin **or** documented portfolio-default exception per D-09."
  artifacts:
    - path: docs/RELEASE.md
      provides: REL-01 operator image matrix + policy
    - path: README.md
      provides: one-line link to release doc under Configuration / Release
    - path: k8s/overlays/prod/kustomization.yaml
      provides: optional `images:` pins (code-side REL-01)
    - path: .github/workflows/ci.yml
      provides: cite-only in RELEASE (no change required)
---

<objective>
Satisfy **REL-01** per `04-CONTEXT.md` **D-07–D-09**: document the **four-image CI build contract**, and make **production** image posture **explicit** (immutable tags/digests in **kustomize** output **or** clearly documented external pinning).

Output: **`docs/RELEASE.md`** (preferred) **or** an equivalent **README** subsection + **`k8s/overlays/prod/kustomization.yaml`** `images:` block aligned with that policy.
</objective>

<context>
@.planning/phases/04-documentation-release/04-CONTEXT.md
@.planning/REQUIREMENTS.md
@.github/workflows/ci.yml
@k8s/base/kustomization.yaml
@k8s/base/qa-agent-deployment.yaml
@k8s/overlays/prod/kustomization.yaml
@README.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/RELEASE.md — build matrix + CI pointer</name>
  <files>docs/RELEASE.md, README.md</files>
  <read_first>.github/workflows/ci.yml (build job, docker build lines)</read_first>
  <behavior>
    - **Table:** Service | Dockerfile path | Image name in CI (`-t …`) | Typical local build command.
    - **CI:** State that **`build`** job **`needs: [test, git-watcher, otel-config]`** and builds **four** images on **push/PR** to `main`/`develop` — cite line range or job name; no need to copy full YAML.
    - **Base vs prod:** Explain **`k8s/base/kustomization.yaml`** `images:` uses **`latest`** for portfolio dev; prod must override (**D-07**).
  </behavior>
  <action>New file `docs/RELEASE.md`; add README link (one line) near Configuration / Kubernetes.</action>
  <verify>
    <manual>Dockerfile paths exist on disk (`glob` or `ls`).</manual>
  </verify>
  <acceptance_criteria>D-07 documentation satisfied.</acceptance_criteria>
  <done>Release doc anchor exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prod kustomize — images: pins for four app services</name>
  <files>k8s/overlays/prod/kustomization.yaml</files>
  <read_first>k8s/base/kustomization.yaml `images:` block, k8s/base/*deployment*.yaml image fields</read_first>
  <behavior>
    - Add **`images:`** entries mirroring **base** names (`qa-agent`, `retriever`, `indexer`, `git-watcher`) with **`newTag`** set to a **semver-style placeholder** (e.g. `0.1.0`) **or** documented **`registry/org/service:<tag>`** pattern — **not** `latest`.
    - Add **comment** in YAML: operators replace tag with CI digest or release version at deploy time; align with **`docs/RELEASE.md`** worked example (`kustomize edit set image` or patch).
    - Ensure `kubectl kustomize k8s/overlays/prod` still succeeds (**D-08**).
  </behavior>
  <action>If team prefers **no** placeholder tag in git, use **`newTag: replaceme`** only if CI forbids — otherwise pick a conservative static tag documented as “example”; prefer explicit `0.1.0` portfolio stamp over `latest`.</action>
  <verify>
    <automated>kubectl kustomize k8s/overlays/prod | rg "image:" | head -40</automated>
  </verify>
  <acceptance_criteria>Rendered prod manifests show non-latest tags for four apps.</acceptance_criteria>
  <done>REL-01 kustomize half satisfied.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Qdrant image — prod pin or explicit portfolio-default doc</name>
  <files>k8s/overlays/prod/kustomization.yaml, docs/RELEASE.md</files>
  <read_first>k8s/base/kustomization.yaml (qdrant image), k8s/base/qdrant-statefulset.yaml</read_first>
  <behavior>
    - **Preferred:** add **`images:`** entry for **`qdrant`** in **prod** overlay with a **pinned semver** (e.g. match a known-good `qdrant/qdrant:vX.Y.Z` from upstream release notes) **or** digest comment.
    - **Acceptable alternative:** **`docs/RELEASE.md`** subsection “Qdrant” stating **`latest` in base is dev-only** and prod operators **must** pin; if chosen, prod overlay may omit qdrant only if verifier text is crystal clear (**D-09**).
  </behavior>
  <action>Prefer pin in prod overlay for stronger REL-01 signal.</action>
  <verify>
    <automated>kubectl kustomize k8s/overlays/prod | rg -i qdrant | rg image</automated>
  </verify>
  <acceptance_criteria>D-09 resolved with evidence in git.</acceptance_criteria>
  <done>Third-party image drift addressed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: RELEASE.md — tie kustomize workflow to CI tags</name>
  <files>docs/RELEASE.md</files>
  <read_first>k8s/overlays/prod/kustomization.yaml after edits</read_first>
  <behavior>
    - Document **exact** `kustomize edit set image` examples for each of the four apps + qdrant (if pinned) matching **`images:`** `name` keys.
    - Mention **GitHub Actions** `github.sha` tagging pattern used in CI builds vs runtime cluster pulls (operators may retag or reference registry digest).
  </behavior>
  <action>Keep DRY — one worked example block + table reference.</action>
  <verify>
    <manual>Follow doc mentally on fresh clone — no missing name keys.</manual>
  </verify>
  <acceptance_criteria>REL-01 verifier question (“what tag, where?”) answerable from docs + YAML.</acceptance_criteria>
  <done>04-02 documentation closed.</done>
</task>

</tasks>

<execution_notes>
- **Wave order:** Run **04-01** first so README/API surface is stable; **04-02** links can reference **`docs/API.md`** ports only if helpful.
- **Qdrant version:** Look up a current stable **v1.x** tag in upstream release notes at execute time — do not invent digests.
- **Verifier:** `kubectl kustomize k8s/overlays/prod` + diff of `image:` lines vs **`docs/RELEASE.md`** table expected.
</execution_notes>

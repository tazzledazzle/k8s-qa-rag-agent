---
phase: 04-documentation-release
plan: 04-01
type: execute
wave: 1
depends_on: []
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "`docs/IMPLEMENTATION.md` opens with a verified status matrix (or equivalent) aligned with GSD Phases 1–3; legacy Day/Phase 6 bullets no longer imply OPERATIONS/API are unwritten."
    - "`docs/API.md` curl examples use compose ports 8000–8003, JSON bodies matching Pydantic models, and document `/ask` dependency on `OPENAI_API_KEY` (502 without key)."
    - "Kubernetes DNS usage for HTTP examples is isolated in a labeled subsection or README cross-link — not silently mixed with localhost compose URLs."
  artifacts:
    - path: docs/IMPLEMENTATION.md
      provides: DOC-01 honest execution picture
    - path: docs/API.md
      provides: DOC-02 operator-facing HTTP examples
    - path: docs/LOCAL-DEV.md
      provides: port parity reference (read-first; patch only if drift)
---

<objective>
Satisfy **DOC-01** and **DOC-02** per `04-CONTEXT.md` **D-01–D-06**: make **`docs/IMPLEMENTATION.md`** reflect **verified** implementation status (vs the legacy 14-day narrative), and align **`docs/API.md`** with **running** compose services and **Pydantic** request models — including honest failure modes and webhook header documentation.

Output: updated **`IMPLEMENTATION.md`** + **`API.md`** only (no **`scripts/export_openapi.sh`** unless a task explicitly adds it as optional scope creep per **D-03**).
</objective>

<context>
@.planning/phases/04-documentation-release/04-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/IMPLEMENTATION.md
@docs/API.md
@docs/LOCAL-DEV.md
@docs/SECURITY-SECRETS.md
@README.md
@services/indexer/models.py
@services/retriever/models.py
@services/qa_agent/models.py
@docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add “Current status (verified)” block to IMPLEMENTATION.md</name>
  <files>docs/IMPLEMENTATION.md</files>
  <read_first>.planning/phases/01-integration-proof/01-VERIFICATION.md, .planning/phases/02-security-boundaries/02-VERIFICATION.md, .planning/phases/03-observability-operations/03-VERIFICATION.md</read_first>
  <behavior>
    - Insert **after** the title/intro and **before** the legacy “Phase 0” timeline: a compact **matrix** (table) with rows such as: Indexer, Retriever, QA agent, Git watcher, K8s base manifests, CI gates, Observability (correlation + OTEL wiring doc); columns **Status** (`Done` / `Partial` / `N/A v1`) + **Evidence** (link to verification file, `tests/`, or path).
    - One short paragraph: legacy sections below are **historical planning** — not unchecked work items.
  </behavior>
  <action>Prefer additive intro + light edits to Phase 6 bullets (next task) over deleting the entire historical doc in one pass.</action>
  <verify>
    <manual>Read rendered markdown top-to-bottom — a new contributor sees truth in first screen.</manual>
  </verify>
  <acceptance_criteria>D-01 satisfied.</acceptance_criteria>
  <done>IMPLEMENTATION.md leads with verified posture.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reconcile stale Phase 6 / checklist / metrics in IMPLEMENTATION.md</name>
  <files>docs/IMPLEMENTATION.md</files>
  <read_first>docs/OPERATIONS.md (exists), docs/API.md (exists)</read_first>
  <behavior>
    - **Phase 6** deliverables: mark **`DESIGN.md`**, **`OPERATIONS.md`**, **`API.md`**, **`ci.yml`** as present where true; remove or strike “→ Phase 6 / unchecked” language that contradicts repo.
    - **Execution Checklist** and **Success Metrics** tables: replace “all unchecked / in progress” with **GSD-accurate** states (or replace table with pointer to the new matrix + `pytest`/CI as regression signal).
    - Do **not** add **`TROUBLESHOOTING.md`** or Helm promises (**D-03**).
  </behavior>
  <action>Minimal prose; avoid rewriting Phases 1–5 technical notes unless factually wrong.</action>
  <verify>
    <manual>grep for “Phase 6”, “→”, unchecked `[ ]` in checklist — should be resolved or annotated.</manual>
  </verify>
  <acceptance_criteria>D-02 satisfied; DOC-01 closure evidence for verifier.</acceptance_criteria>
  <done>Legacy checklist no longer lies about doc existence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: API.md — prerequisites, compose vs Kubernetes, body accuracy</name>
  <files>docs/API.md</files>
  <read_first>docker-compose.yml, services/*/models.py, docs/LOCAL-DEV.md</read_first>
  <behavior>
    - **Prerequisites:** `docker compose up`, required env vars for **`/index`** (GitHub token if private), **`/ask`** (`OPENAI_API_KEY` for successful LLM path).
    - **Per service:** confirm paths, verbs, and example JSON keys match **Pydantic** defaults (`branch` default `main` on indexer if applicable, optional `session_id`, `language`, glob lists).
    - **Subsection “Kubernetes (cluster DNS)”:** one example pattern (e.g. `http://indexer-service:8000/health`) with pointer to **`README.md`** Configuration — do not duplicate full service list (**D-04**).
  </behavior>
  <action>Keep tables; extend with small “Notes” under each service if needed.</action>
  <verify>
    <manual>Diff example JSON against `services/*/models.py` field names.</manual>
  </verify>
  <acceptance_criteria>D-04, D-05 partial (field names).</acceptance_criteria>
  <done>API doc structure ready for curl honesty pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: API.md — /ask failure note + webhook headers cross-link</name>
  <files>docs/API.md</files>
  <read_first>docs/SECURITY-SECRETS.md, services/git-watcher (webhook route behavior if needed)</read_first>
  <behavior>
    - Under QA agent: document **`POST /ask`** returns **502** when the agent/LLM path fails (e.g. missing key, upstream error) — do not imply 200 with body unless optional smoke with key is explicitly labeled.
    - Under Git watcher: list required/typical headers (`X-Hub-Signature-256`, `X-GitHub-Event`, JSON body); note **push-only** processing and link **`docs/SECURITY-SECRETS.md`** for secret handling (**D-06**).
  </behavior>
  <action>No secret literals in examples.</action>
  <verify>
    <manual>Security reviewer skim — no pasted tokens.</manual>
  </verify>
  <acceptance_criteria>D-05, D-06 satisfied; DOC-02 ready for verify-phase curl drill.</acceptance_criteria>
  <done>DOC-02 author-facing complete pending optional live curl in verify.</done>
</task>

</tasks>

<execution_notes>
- **Order:** Task 1 → 2 (IMPLEMENTATION), then Task 3 → 4 (API.md).
- **Optional (defer):** `scripts/export_openapi.sh` — only if execute-phase has spare capacity; not required for roadmap **04-01** text.
- **Verify-phase hint:** `/gsd-verify-phase 4` may run `curl` against compose for `/health` and `/search`; `/ask` may be health-checked via OpenAI availability note rather than mandating spend.
</execution_notes>
